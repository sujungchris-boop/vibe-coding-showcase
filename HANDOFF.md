# 세션 인수인계 (2026-07-25 기준)

> 새 세션 시작 시 `CLAUDE.md`(아키텍처·작업 스타일) → 이 문서(현재 상황) 순으로 읽을 것.
> 이 문서는 "지금 어디까지 왔고 무엇을 지켜보는 중인지"의 스냅샷 — 오래되면 갱신한다.

## 현재 상태
- 라이브: https://vibe-coding-showcase-ten.vercel.app (main push = 자동 배포, 현재 HEAD 기준 안정)
- 학생 9명 활동 중. 작품 12개(+시연작 '복음의 불길'). 데이터 원본은 Firestore(레포 아님).
- 스튜디오 파이프라인 최종형: **부분 수정(````edit`) + append-only 히스토리 + 프롬프트 캐싱 + Haiku 기본 라우팅**. 상세는 CLAUDE.md.

## 최근 결정·사건 요약 (시간순)
1. 부분 수정 도입으로 작품 크기 제한 해소 (학생들이 실제로 100KB+까지 디벨롭 중).
2. 비용 절감 배포(캐싱+라우터): 호출당 ~$0.16 → ~$0.02. **모델 전환(GLM/Kimi)은 검토 후 보류** — 캐싱 적용 시 실효 단가 이점이 작고 전환 리스크(수정 프로토콜 재검증·한국어 톤·중국계 API 데이터 전송)가 큼.
3. "코드 잘림" 오탐 수정: 감지를 형식이 아닌 완성 여부(`</html>`·REPLACE 종결) 기준으로. `stopReason`이 usage에 기록됨.
4. 작품 버전 히스토리 + admin 롤백 가동 중 (Firestore 규칙 게시 완료).
5. 학기 성적 평가 완료 — **성적 파일·평가 문장은 학생 개인정보라 레포에 없음** (소유자 로컬 Downloads 보관).

## 지켜보는 중 (다음 세션이 확인할 것)
- **비용 추세**: admin "AI 사용량"에서 Haiku+캐시읽기 위주로 쌓이는지, Sonnet 정가 입력이 재등장하지 않는지. 이상 시 usage 컬렉션의 `stopReason`·모델 분포로 진단.
- **잘림 오탐 재발 여부**: 학생이 "잘렸다" 호소하면 usage의 stopReason(end_turn=오탐 가능성 / max_tokens=진짜 한도)부터 볼 것.
- **수정 적용 실패율**: "수정 위치가 어긋나" 메시지가 잦으면 applyEdits 매칭 보강 검토.

## 다음 후보 작업 (요청 시)
- 학생별 일일 사용 상한 (비용이 다시 부담될 때의 1순위 카드)
- 세특(평가 문장) 파일 작성 요청 시: 소유자 로컬 `~/Downloads/성적평가_근거문장.md` 참고 (레포 밖)
- '복음의 불길' v1.0+ 확장(사운드·이벤트) 또는 학생 예시용 튜토리얼화
