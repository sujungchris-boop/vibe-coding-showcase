# 바이브코딩 쇼케이스 — 개발 환경 & 제작 가이드

> 학생 작품(= AI 스튜디오에서 만드는 결과물)을 **기획·제작할 때 알아야 할 환경과 제약**.
> 기획안은 이 문서의 제약 안에서 짜야 실제로 만들어집니다. (내부 상세는 `CLAUDE.md` 참고)

## 스택 한눈에
- 순수 **HTML/CSS/JS**. 빌드 도구·프레임워크 **없음**. (각 페이지 = 단일 HTML + `<script type="module">`)
- 호스팅: **Vercel**(정적) + 서버리스 함수 1개(`api/generate.js`, AI 스튜디오용 LLM 호출, **SSE 스트리밍**).
- 데이터: **Firebase Firestore** / 프로필 사진: **Storage**. 별도 백엔드 없음.
- 배포: GitHub `main` push → **Vercel 자동 배포** → https://vibe-coding-showcase-ten.vercel.app

## 학생 작품이 실행되는 방식 (기획 핵심 제약)
작품 하나 = **자체 완결형 HTML 파일 1개** (`<!DOCTYPE html> … </html>`).
`iframe sandbox="allow-scripts" srcdoc`로 렌더된다.

**된다 ✅**
- CDN 라이브러리: p5.js · Three.js · Phaser · GSAP · Tone.js · D3 등 (`<script src="https://cdn…">`)
- Canvas / SVG / WebGL, 애니메이션, 사운드(Tone.js 합성 또는 CDN 오디오 URL)
- localStorage(작품 내 저장), fetch(공개 API) 등 브라우저 기능

**안 된다 ❌**
- npm install·빌드가 필요한 다중 파일 프로젝트(Vite/Next/React 빌드 등)
- 파일(이미지·오디오·폰트) **업로드** → 에셋은 **인라인(SVG·data URI)·CDN·합성**으로
- 작품 자체의 서버/DB → 진짜 멀티플레이어·서버 저장은 불가 (작품은 **클라이언트만**)

**크기 한계 (중요)**
| 한계 | 값 | 의미 |
|---|---|---|
| Firestore 저장 | `fullHtml` **< 500KB** | 작품 최종 크기 상한 (스튜디오 미리보기에 크기 게이지 표시) |
| 스튜디오 "전체 생성" 1회 | **max_tokens 64,000** (≈ 코드 200KB) | 새 작품·전면 개편 때만 해당 |
| 스튜디오 "수정" | **크기 무관** | 수정은 부분 수정(````edit` SEARCH/REPLACE)으로 바뀐 부분만 출력·적용 → 큰 작품도 계속 디벨롭 가능 |

→ 처음 만들 땐 **단계별로 키우는 게** 여전히 좋은 습관. (예: 게임을 v0.1 코어 → v0.5 시각 → v0.8 이벤트 → v1.0 완성)

## AI 스튜디오 (제작 도구)
- 채팅으로 요청 → AI가 통짜 HTML 생성(**실시간 스트리밍**) → 라이브 미리보기 → 반복 수정 → 게시.
- **모델 라우터:** 단순 = Claude **Haiku 4.5**(가성비) / 복잡(3D·물리·시뮬·게임 등) = **Sonnet 4.6** 자동 승급.
- **디자인씽킹 코치:** 공감 → 발상 → 만들기 → 개선 단계 띠 + AI 단계별 코칭.
- 코드 보기 / 복사 / 다운로드 지원.

## 잘 맞는 작품 / 무리한 작품 (기획 가늠자)
- **잘 됨:** 2D 게임 · 전략/시뮬레이션 · 인터랙티브 시각화 · 교육 도구 · 퀴즈 · 보드게임 · 애니메이션
- **주의(무겁거나 쪼개야 함):** 수천 줄 단일 대작, 정밀 세계지도 등 무거운 에셋(→ 스타일라이즈드로 단순화), 대규모 3D 씬
- **불가:** 서버가 필요한 실시간 멀티플레이어, 파일 업로드 기반 기능

## 플랫폼 기능 현황 (작품 외 사이트)
갤러리(인기 표시) · 작품 상세(❤️⭐👏 반응 · 피드백 댓글) · 학생 프로필(받은 응원) · 제출/내작품 관리(로그인 유지) · 관리자(학생 계정 생성 · 비번 재설정 · 작품 수정) · 모바일/태블릿 최적화 · 아기자기 파스텔 테마.
